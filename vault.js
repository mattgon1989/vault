
//   create a message for the user describing the vault and the combination
//   calculate the first combination number by adding 5 and 5
//   calculate the second combination number by multiplying 4 and 10
//   calculate the third combination number by subtracting 40 and 1
//   show the message and the vault combination to the user

// create message for user
const messageForUser = 'You have received this message because you have been chosen to open an important vault. Here is the secret combination:';

// calculate first combination number
const num1 = 5 + 5;

// calculate second combination number
const num2 = 4 * 10;

// calculate third combination number
const num3 = 40 - 1;

// create alert dialog with message and vault combination
alert(messageForUser + ' ' + num1 + ' ' + num2 + ' ' + num3);
